services:
  - type: web
    name: interviewos-backend
    env: python
    rootDir: apps/backend
    buildCommand: "pip install -e . && alembic upgrade head"
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: interviewos-db
          property: connectionString
      - key: GEMINI_API_KEY
        sync: false
      - key: SECRET_KEY
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: ALLOWED_ORIGINS
        value: "https://interviewos.vercel.app"
    plan: free

databases:
  - name: interviewos-db
    databaseName: interviewos
    user: admin
    plan: free
